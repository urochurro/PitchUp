import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#083767',
    // secondary: '#414757',
    secondary: '#fff',
    error: '#f13a59',
  },
};
